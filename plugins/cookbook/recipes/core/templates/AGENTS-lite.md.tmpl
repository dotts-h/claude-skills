# AGENTS.md — {{project_name}}

{{project_description}}

Thin pointer; the rules live in [docs/CONVENTIONS.md](docs/CONVENTIONS.md) — read it
before acting. Gates: `{{lint_command}}` and `{{test_command}}` before push; CI green
before merge. Branch from `{{default_branch}}`; never commit to it directly.
