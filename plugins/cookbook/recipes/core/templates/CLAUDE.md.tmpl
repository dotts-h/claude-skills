# CLAUDE.md

@AGENTS.md

The canonical agent guidance for {{project_name}} lives in [AGENTS.md](AGENTS.md)
(imported above), so every coding agent — whatever the host — reads one set of rules.
Do not add rules here; add them to the doc that owns them (see AGENTS.md for the map).
